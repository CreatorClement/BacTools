% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/query_all.R
\name{search_bactools}
\alias{search_bactools}
\title{Query All Repositories (CRAN, Bioconductor, and GitHub)}
\usage{
search_bactools(
  tags = NULL,
  additional_tags = NULL,
  use_defaults = TRUE,
  include_cran = TRUE,
  include_bioc = TRUE,
  include_github = TRUE,
  github_token = NULL,
  github_per_page = 30,
  github_pages = 2,
  add_citations = FALSE,
  return_df = TRUE
)
}
\arguments{
\item{tags}{Character vector of search tags used across CRAN, Bioconductor, and GitHub (default: default_bacterial_tags)}

\item{additional_tags}{Additional tags to append to the search (default: NULL)}

\item{use_defaults}{Logical, whether to use default tags (default: TRUE)}

\item{include_cran}{Logical, include CRAN packages (default: TRUE)}

\item{include_bioc}{Logical, include Bioconductor packages (default: TRUE)}

\item{include_github}{Logical, include GitHub repositories (default: TRUE)}

\item{github_token}{GitHub Personal Access Token (default: NULL, uses GITHUB_PAT env var)}

\item{github_per_page}{Results per page for GitHub (default: 30)}

\item{github_pages}{Pages to retrieve per tag (default: 2)}

\item{add_citations}{Logical, retrieve citation counts for CRAN/Bioc packages (default: FALSE)}

\item{return_df}{Logical, return as data frame instead of list (default: TRUE)}
}
\value{
Data frame or list of package/repository information from all sources
}
\description{
Search all three repositories and combine results into a unified catalog
}
\examples{
\dontrun{
# Query all repositories with defaults
all_tools <- search_bactools()

# Add custom terms to all searches
all_tools <- search_bactools(
  additional_tags = c("sequencing", "assembly", "phylogenetics", "metagenomics")
)

# Query only GitHub with custom tags
github_only <- search_bactools(
  tags = c("viral-genomics", "phage"),
  use_defaults = FALSE,
  include_cran = FALSE,
  include_bioc = FALSE,
  github_token = "ghp_xxxx"
)

# Full catalog with citation counts
full_catalog <- search_bactools(
  add_citations = TRUE,
  github_token = Sys.getenv("GITHUB_PAT")
)
}
}
